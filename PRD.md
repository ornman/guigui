# PRD: SchoolAutoLogin 桌面应用

> 校园网（Dr.COM）自动登录桌面工具，带 GUI 界面、系统托盘、轮询断网重连、定时登录。

---

## 1. 背景与目标

### 现状

当前 `auto_login.py` 是纯脚本，通过 `config.json` 配置，PyInstaller 打包为 exe。用户需要手动编辑 JSON 文件来修改配置，无 GUI，定时登录依赖 Inno Setup 安装时固定创建的 Windows 任务计划。

### 目标

做一个轻量 Windows 桌面应用（单 exe），用户通过 GUI 完成所有配置和操作，无需手动编辑任何文件。

**设计原则：**

- 轻量：仅依赖 Python 标准库 + `customtkinter`（一个 pip 包，基于 tkinter）
- 单进程：GUI、托盘、轮询都在一个 exe 里
- 零端口：原生桌面窗口，禁止 Web Server / localhost 方案

---

## 2. 技术栈

| 组件 | 选择 | 理由 |
|------|------|------|
| GUI 框架 | `customtkinter` | 现代外观，基于 tkinter，单 pip 包 |
| 系统托盘 | `pystray` + `Pillow` | 轻量，纯 Python |
| 后台任务 | `threading.Thread` | 标准库，无需额外依赖 |
| 定时登录 | Windows 任务计划程序（`schtasks`） | 系统原生，关了 app 也能执行 |
| 开机自启 | 注册表 `HKCU\...\Run` | 标准做法 |
| 通知 | PowerShell WinRT（现有实现） | 已验证可用 |
| 打包 | PyInstaller + Inno Setup | 与现有流程一致 |
| 配置存储 | `config.json` | 与现有格式兼容 |

**新增 pip 依赖：** `customtkinter`, `pystray`, `Pillow`

---

## 3. 用户流程

### 3.1 首次使用

```
双击 exe → 打开主窗口
→ 填写：运营商（下拉选择）、学号、密码
→ 点击「保存」→ 配置写入 config.json
→ 点击「立即登录」→ 执行登录
```

### 3.2 日常使用

```
双击 exe → 主窗口（已加载配置）
→ 手动登录 / 修改设置 / 最小化到托盘
```

### 3.3 后台模式（轮询开启时）

```
关闭主窗口 → 最小化到系统托盘
→ 后台线程每 N 秒检测网络
→ 断网 → 自动执行登录流程
→ 右键托盘图标 → 显示状态 / 打开主界面 / 退出
```

### 3.4 定时登录（独立于 app）

```
在设置中配置时间（如 06:55）→ app 创建 Windows 任务计划
→ 每天 06:55 系统自动执行 exe --silent
→ exe 以静默模式运行（无窗口），仅通知结果
→ 无需 app 在运行
```

---

## 4. 功能需求

### 4.1 主界面

#### 登录区域

| 字段 | 控件 | 说明 |
|------|------|------|
| 运营商 | 下拉框 | 选项：中国电信、中国联通、校园用户 |
| 学号 | 文本框 | 必填 |
| 密码 | 密码框 | 必填，带显示/隐藏切换 |

#### 操作按钮

| 按钮 | 行为 |
|------|------|
| 立即登录 | 保存配置 → 切换 WiFi → 检测网络 → 执行登录 → 显示结果 |
| 保存配置 | 仅保存 config.json，不执行登录 |

#### 状态显示

- 当前状态文本：未配置 / 已配置 / 登录中... / 已连接 / 连接失败
- 最近登录时间

### 4.2 设置面板

#### 网络

| 设置项 | 控件 | 默认值 | 说明 |
|--------|------|--------|------|
| 校园网 WiFi 名称 | 文本框 | 空 | 登录前自动切换到该 WiFi，留空则跳过 |

#### 轮询断网重连

| 设置项 | 控件 | 默认值 | 说明 |
|--------|------|--------|------|
| 启用轮询 | 复选框 | 关闭 | 开启后关闭窗口最小化到托盘 |
| 检测间隔 | 数字输入（秒） | 30 | 多久检测一次网络连通性 |

#### 定时登录

| 设置项 | 控件 | 默认值 | 说明 |
|--------|------|--------|------|
| 启用定时登录 | 复选框 | 关闭 | 创建 Windows 任务计划 |
| 执行时间 | 时间选择器 | 06:55 | HH:MM 格式 |

#### 开机自启

| 设置项 | 控件 | 默认值 | 说明 |
|--------|------|--------|------|
| 开机自动启动 | 复选框 | 关闭 | 写入注册表 Run 键 |

#### 通知

| 设置项 | 控件 | 默认值 | 说明 |
|--------|------|--------|------|
| 显示通知 | 复选框 | 开启 | 登录成功/失败时弹出 Windows 通知 |

### 4.3 系统托盘（轮询开启时）

**触发条件：** 轮询开关打开 + 用户关闭主窗口

**托盘行为：**

- 图标状态：正常（绿色）/ 断网（红色）/ 登录中（黄色）
- 右键菜单：
  - `打开主界面` → 重新显示主窗口
  - `立即登录` → 手动触发一次登录
  - `状态：已连接 / 断网中` → 只读显示
  - `退出` → 完全退出 app（停止轮询）

### 4.4 静默模式（定时任务用）

```
命令行: SchoolAutoLogin.exe --silent
```

- 不显示任何窗口（无 GUI、无控制台）
- 读取 config.json 执行登录
- 结果通过 Windows 通知反馈
- 与现有 `auto_login.py` 的行为完全一致

### 4.5 配置文件

与现有 `config.json` 格式兼容，新增字段：

```json
{
    "url": "http://10.1.2.3",
    "wifi_ssid": "",
    "operator": "中国电信",
    "username": "",
    "password": "",
    "max_retries": 3,
    "retry_interval_seconds": 5,
    "polling_enabled": false,
    "polling_interval_seconds": 30,
    "scheduled_login_enabled": false,
    "scheduled_login_time": "06:55",
    "auto_start": false,
    "notification_enabled": true
}
```

---

## 5. UI 设计规范

### 5.1 设计风格：毛玻璃 × 新粗野主义

两种风格看似矛盾，实则互补：毛玻璃提供层次感和呼吸感，新粗野主义提供力量感和辨识度。

**核心公式：** 毛玻璃质感卡片 + 粗暴边框阴影 + 大胆排版 = 不像 AI 生成的东西

### 5.2 色彩系统

```
背景层    #0c0c14    深空黑，带微蓝调
卡片层    #181825    毛玻璃底色，比背景提亮一级
卡片边框  #2e2e3e    可见但不抢，2px 实线
主强调色  #7c3aed    电紫，用于主按钮和高亮
次强调色  #f59e0b    琥珀，用于警告和次要操作
成功色    #10b981    翠绿，用于状态指示
错误色    #ef4444    亮红，用于错误状态
文字主色  #f0f0f5    近白，高对比
文字次色  #6b6b80    灰调，辅助信息
阴影色    #000000    模拟偏移阴影，4px offset
```

> 不用蓝紫渐变、不用中性灰白、不用 Tailwind 默认色。这个配色有态度。

### 5.3 布局原则

```
┌─────────────────────────────────────────┐
│  SchoolAutoLogin            ─  □  ✕    │  ← 标题栏：无系统装饰，自绘
├─────────────────────────────────────────┤
│                                         │
│  ┌─────────────────────────────────┐    │
│  │  ╔═══════════════════════════╗  │    │
│  │  ║  ① 登录      ② 设置      ║  │    │  ← Tab 切换，粗体大字
│  │  ╚═══════════════════════════╝  │    │
│  │                                 │    │
│  │  运营商  ┌──────────────────┐   │    │
│  │         │ 中国电信        ▼ │   │    │  ← 下拉框：2px 边框
│  │         └──────────────────┘   │    │
│  │                                 │    │
│  │  学号    ┌──────────────────┐   │    │
│  │         │                  │   │    │  ← 输入框：暗底 + 亮边
│  │         └──────────────────┘   │    │
│  │                                 │    │
│  │  密码    ┌──────────────────┐   │    │
│  │         │ ●●●●●●●      👁 │   │    │  ← 密码框 + 可见切换
│  │         └──────────────────┘   │    │
│  │                                 │    │
│  │  ┌─────────────────────────┐   │    │
│  │  │      立 即 登 录         │   │    │  ← 主按钮：填充色 + 粗边框
│  │  └─────────────────────────┘   │    │
│  │     ↕ 4px                     │    │  ← 按钮阴影偏移
│  │  ┌─────────────────────────┐   │    │
│  │  │      保 存 配 置         │   │    │  ← 次按钮：透明底 + 亮边
│  │  └─────────────────────────┘   │    │
│  │                                 │    │
│  │  ● 已连接  2024-01-15 12:30    │    │  ← 状态栏：小字 + 彩色圆点
│  └─────────────────────────────────┘    │  ← 整个卡片：圆角 + 粗边框
│                                         │
└─────────────────────────────────────────┘
```

### 5.4 组件规范

#### 卡片（毛玻璃感）

```
- 背景: #181825（比底色亮一级，模拟毛玻璃透光）
- 圆角: 12px
- 边框: 2px solid #2e2e3e
- 内边距: 24px
- 不追求真实模糊，用颜色层级制造深度感
```

#### 按钮（新粗野主义）

```
主按钮:
  - 背景: #7c3aed（电紫填充）
  - 边框: 3px solid #5b21b6（深紫，比填充色暗一级）
  - 圆角: 8px
  - 阴影: 4px 4px 0 #000（硬偏移，无模糊，关键特征）
  - 字体: 粗体 14px
  - Hover: 上移 2px + 阴影变 6px（弹起感）
  - Active: 下移 2px + 阴影变 2px（按下感）

次按钮:
  - 背景: transparent
  - 边框: 2px solid #7c3aed
  - 其余同上
```

#### 输入框

```
- 背景: #0f0f1a（比卡片暗，凹陷感）
- 边框: 2px solid #2e2e3e
- 圆角: 6px
- Focus 时边框: #7c3aed
- 文字色: #f0f0f5
- Placeholder 色: #4a4a5e
- 字体: 14px
- 高度: 40px
```

#### 状态指示

```
●  #10b981  已连接
●  #f59e0b  检测中
●  #ef4444  断网
●  #6b6b80  未配置

圆点 8px + 文字 12px 灰色
```

### 5.5 排版

```
标题:    "Microsoft YaHei" Bold 28px    用于窗口标题
章节:    "Microsoft YaHei" Bold 18px    用于 Tab 名称
标签:    "Microsoft YaHei" 600 13px     用于字段标签（学号、密码等）
正文:    "Microsoft YaHei" 400 14px     用于内容文字
辅助:    "Microsoft YaHei" 400 12px     用于状态栏、提示文字
按钮:    "Microsoft YaHei" Bold 14px    用于按钮文字
```

### 5.6 动效

```
- 按钮 hover/active: 阴影偏移变化（CSS transition 感，tkinter 里用 after 模拟）
- Tab 切换: 无动画，直接切换内容（干脆，符合粗野主义）
- 状态变化: 圆点颜色即时切换，不渐变
- 窗口打开: 无特殊动画
```

### 5.7 不要做的事

```
❌ 不用渐变背景
❌ 不用圆角超过 16px（太圆润 = 太 AI）
❌ 不用微阴影 / box-shadow 模糊（软阴影 = 太普通）
❌ 不用 emoji 做 icon
❌ 不用 "欢迎"、"开始您的旅程" 之类的 AI 套话
❌ 不用纯白底 + 灰边框的默认样式
❌ 标题不加 glow 发光效果
```

---

## 7. 项目结构

```
auto-login/
├── src/                    # 应用源码
│   ├── __init__.py
│   ├── app.py              # 应用入口、主窗口
│   ├── login.py            # 登录逻辑（从 auto_login.py 重构）
│   ├── tray.py             # 系统托盘
│   ├── poller.py           # 轮询检测线程
│   ├── scheduler.py        # 定时任务管理（schtasks）
│   ├── config.py           # 配置读写
│   ├── notify.py           # Windows 通知
│   ├── wifi.py             # WiFi 管理
│   └── ui/                 # GUI 组件
│       ├── __init__.py
│       ├── login_tab.py    # 登录面板
│       └── settings_tab.py # 设置面板
├── assets/
│   └── icon.ico            # 应用图标（用于窗口 + 托盘）
├── auto_login.py           # 旧脚本（保留，用于 --silent 模式或兼容）
├── config.json
├── main.py                 # 启动入口
├── build.bat
├── auto_login.spec         # PyInstaller 配置（更新）
├── setup.iss               # Inno Setup 配置（更新）
└── tests/
    ├── test_login.py
    ├── test_config.py
    └── test_poller.py
```

---

## 8. 运行模式总结

| 模式 | 启动方式 | GUI | 托盘 | 用途 |
|------|---------|-----|------|------|
| 交互模式 | `SchoolAutoLogin.exe` | ✅ 主窗口 | 轮询开启时 | 用户手动操作 |
| 静默模式 | `SchoolAutoLogin.exe --silent` | ❌ | ❌ | 定时任务调用 |
| 托盘模式 | 关闭主窗口（轮询开启时） | ❌ | ✅ | 后台断网重连 |

---

## 9. 非功能需求

| 项目 | 要求 |
|------|------|
| 启动速度 | 冷启动 < 3 秒 |
| 内存占用 | 空闲时 < 50 MB |
| 打包体积 | exe < 30 MB |
| 兼容性 | Windows 10/11 64-bit |
| 中文界面 | 所有 UI 文本为中文 |

---

## 10. 实现优先级

### P0 — MVP（第一版）

1. 主界面：登录表单 + 立即登录
2. 配置读写（config.json）
3. `--silent` 静默模式
4. 通知功能

### P1 — 核心功能

5. 设置面板（WiFi、轮询、定时、开机自启、通知开关）
6. 系统托盘 + 轮询断网重连
7. 定时任务管理（创建/删除 schtasks）
8. 开机自启（注册表）

### P2 — 优化

9. 托盘图标状态（颜色变化）
10. 日志查看面板
11. 网络状态实时显示
