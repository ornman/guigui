# 桂桂隔离测试床 · bind 解绑后阶梯补测(2026-09-06 晚)

> 目标:补上 PRD §8.5.4 唯一待测项 — 测试账号 …224 解绑后的
> **「在线注销 → 等翻转 → 真登」验证阶梯全流程**(对应 `flows/guigui-first-run.workflow` 主路)。
> 此前(2026-09-06 白天)该账号从墙口登录被 `bind userid error` 拦(密码已证正确),
> T3 真登录 / T6 阶梯全流程被阻,待自助平台解绑后补测。

## 前置体检(执行前已验证,全只读)

| 项 | 实测 | 结论 |
|---|---|---|
| 网口链路 | 以太网 IF18 = 172.16.0.1/21(墙口 DHCP),Connected | **已插** |
| 跃点锁定 | WLAN 2 = 10 / 以太网 = 60(均关 AutomaticMetric) | 在岗 |
| 主机路由 | `10.1.2.3/32 → 172.16.0.254 @ IF18`,RouteMetric 1;Find-NetRoute 走以太网 | **已钉** |
| WiFi 通道 | 192.168.31.100/24(小米路由上游全屋会话 …209) | 不碰 |
| 测试脚本 | `%TEMP%\guigui-live-test.py`(只 import drcom/detect,GUIGUI_DATA_DIR 指临时目录) | 在位 |
| shell 权限 | 非管理员 — 路由增删需 UAC,本次不动路由 | 已知 |

## 铁律

1. 一切 `10.1.2.3` 请求只走网口(主机路由已钉,OS 层强制);**WiFi 绝不碰**。
2. **金丝雀**:任何 chkstatus 若返回 `2025000000001`(全屋会话)= 路由失效命中 WiFi → **立即停止一切请求**并报告。
3. 有副作用的请求(登录/注销)之间间隔 **≥4 秒**;`error5 waitsec` 限流时等 4s 重试一次,不硬打。
4. 密码只经 `GUIGUI_TEST_PW` 环境变量现场传入 — **不进 git / 文档 / memory**。
5. 服务器返回不认识的格式 → 原样记录,不猜原因(PRD 4.1.2 原则)。
6. 请求预算:全程 ≤2 次登录 + ≤2 次注销 + 必要只读探测(学校侧风控未知,能省则省)。

## 执行步骤

- **S0 `status`(只读)**:probe 四态 + chkstatus + 门户标题 + 注销端点。
  预期:未登录(chkstatus=None,上次收尾已注销)。金丝雀校验。
  若已登录且是 …224 → 直接进 S2;若是他人学号 → 如实记录,仍走 S2
  (= PRD 4.1.2「线上是别的学号,验证时会先注销它」分支的实景)。
- **S1 bind 判定(真登一次,operator=校园用户 '')**:
  - `success` → **已解绑**,进 S2;
  - `bind userid error` → **未解绑**,记录原文,跳 S4 收尾(阶梯仍被阻);
  - `error1 / error2 / 其他` → 记录原文即停(不猜、不换运营商连试);
  - `waitsec` → 等 4s 重试一次。
- **S2 `ladder`(补测主体,仅 S1 成功时)**:注销 → 翻转轮询(≤8×0.5s)→ 4s 节流 →
  真登(waitsec 再等 4s 重试一次)→ 终态 chkstatus。
- **S3 隔离复核**:S2 终态 chkstatus = …224 即证明墙口独立会话与 WiFi 全屋 …209 并存(不再单独发请求,S2 输出为准)。
- **S4 收尾**:注销墙口会话 + 确认翻转。**主机路由保留不删**(偏差见下)。
- **S5 回填**:结果写回本文件「结果」节 + PRD §8.5.4(+§4.1.2 如有新事实)+ memory;git 提交。

## 与记忆收尾铁律的已知偏差(如实声明)

记忆「测完注销墙口会话 + 删临时路由」;本次**注销照做、路由保留**,理由:
①当前 shell 非管理员,删路由需 UAC 弹窗;②路由钉着 = 10.1.2.3 永远走网口,
客观上防止将来误经 WiFi 命中全屋会话(保护性);③AC-12「06:50 开门锚点」窗口期验证
仍待做,同一测试床还要用。拆除命令(用户提权执行):
`Remove-NetRoute -DestinationPrefix 10.1.2.3/32 -InterfaceIndex 18 -Confirm:$false`。

## 结果(执行后回填)

- [ ] S0 初始态:
- [ ] S1 bind 判定:
- [ ] S2 阶梯(注销/翻转耗时/节流/真登/终态):
- [ ] S3 隔离:
- [ ] S4 收尾:
- [ ] 结论与回填:
