# 前端交互重构计划 v2(白板定稿对齐版)2026-09-10

> **状态**:已拍板,待执行。本版按 **2026-09-10 飞书白板定稿**(两轮终审闭环,69 节点/107 边,无死端/无孤儿/层序严格)全面改写 v1(2026-09-09)。
> **正本关系**:**业务流程唯一正本 = 白板**(`docs/prd/flows/guigui-user-flow.dsl.json` ⇄ 飞书文档 DAHDdncH1o0mYXxf2QachTKYn5g / 板 QCbHwDewwhCk3xbpxVlc6SJZnnb)。本文与白板冲突时,**以白板为准**;白板改动须按推送配方同步(见 flows 目录推送配方文档)。
> **v1→v2 变更摘要**:P0-7 改写(不自动重登,改调起状态页+快登);P1-8/P1-10 改写(探测后台常驻/状态页=问题中枢);新增 P1-12~P1-17(快登/日常页两态/检验自适应/故障带四链/成功页矩阵/任务①②生命周期);补写 v1 缺失的 P1-1~P1-7 正文;J 通知策略/K 模块复用表升为硬约束;P1-1~P1-7 编号不变防错引。
> **v1 输入**(保留记录):2026-09-09 四视角审计 10 实锤(4 P0/4 P1/2 P2)+ 内置浏览器逐场景实测;46 条交互链全景评估(21 健康/12 bug 级/7 链级/6 待裁)。

> **拍板记录第一批**(2026-09-09,用户四问拍板):
> ① waiting 态修法 = **排查页感知 waiting**(不新增主页第四态);
> ② 架构收口范围 = **三件全做**(表单合一 / 路由数据化 / store 收口),不拆文件;
> ③ 拦截终态页 = **加 quiet 次按钮「先不管,回主页」**;
> ④ 事件打断策略 = **填表中不打断**(表单有已键入内容时,unreachable 事件不拽人去排查页,就地状态行;终态页/详情页维持统一跳)。
> **拍板记录第二批**(2026-09-09 晚,用户业务流程图评审):
> ⑤ **开门判断全局删除**——waiting 前端视同不可达,唯一落点 v-guide;bootWait 等门页删除,06:50 文案全部下线(P0-3 改写,P0-8 作废);
> ⑥ 节流分支由 agent 补进流程正本;
> ⑦ 登录页保留返回键;日常被拒分流:密码不对/学号运营商错→提示进登录页;绑定受限/学号在别设备→状态页提示「是否更换账号?」,不换→返回日常页(C);
> ⑧ **大头状态页(非首装独有,全屏红,bot 沮丧)与横幅并存**,登录页返回落点=带错误横幅的日常页;5s 探测=10.1.2.3 连通性检查不触发登录(D/P1-10);
> ⑨ 验证器专用计划任务机制**先不管**(park,G 章);后端 diagnose 已做过,入口不重做;
> ⑩ 用户业务流程图(飞书白板)作为前端业务流程**正本**;agent 画的 as-is 图降级为审计底稿。
> **拍板记录第三批**(2026-09-10,白板定稿轮,编号续):
> ⑪ **大头状态页=问题中枢**(三心境合一:蓝变红⑧/日志矛盾/沮丧⑦);**日常页仅两态**(全部正常/带横幅返回自带);**后端返回五态**(四拒+节流·中性);手动快登入口=状态页「快速登录」;**探测后台常驻**,发现问题系统直接调起状态页;巡检「网」维度=读探测结果;
> ⑫ **检验=一份自适应服务**(未登录→真登;已登录→chkstatus 对账;**禁注销全量**——共享会话/断网风险;密码盲区由下一未登录时刻自然暴露);
> ⑬ **故障带四维一行一链**:库(重建×3→降级=切备份库JSON)/任务(拦截页重建)/日志(实况分诊三路汇状态页)/程序(自动修复→失败反馈附重装指引);自愈成功统一回巡检复查;
> ⑭ **配置模式自成一路**:存凭据→建任务→任务①建成→「保存配置成功」页→返回不可达排查页(按当前状态);删除「以配置模式→提交表单」矛盾边;
> ⑮ **任务①②生命周期**:建成?+通知 / 登上网?;创建失败(杀软)→拦截页;登录失败→🔔带原因→**五态分流(复用🅛)**;明早 7 点任务跑=进非首装链路;
> ⑯ **彩带=用户在场的验证通过**(首装+日常改密;GUI 低频,在场时刻都值得庆祝);明早无人值守成功=通知不弹彩带;
> ⑰ **通知**:成功默认开(可关);连不上类最终失败也发;矛盾态不发;凭证库降级发;冷却=同类 30 分钟合并、每类每天 ≤1 条;通知点击=深链(P1-11);**复用硬约束**(🅛🔑📄📡🔔 实现各一份,见 K 章)。

---

## 执行约定(新对话零上下文必读)

1. **对象文件**:`guigui/app/static/index.html`(下称「主文件」)、`app.js`(桥接,基本不动)、`dev/mock.js`(契约 mock,只读参照)、`dev/state-gallery.html`(摆拍画廊,同步义务)、`docs/prd/flows/guigui-user-flow.dsl.json`(业务流程正本,与飞书白板双向同步)。
2. **定位方式**:本计划引用的行号是审计时点,**执行时以函数名定位**(如 `quickLogin` / `renderBanners` / `net:state` 处理器);动手前先重读主文件相关段——**用户会亲手改文件试文案,以磁盘上现状为准**。
3. **测试基准**:
   - 内置浏览器(或 devshell)开 `http://…/index.html?dev=1&scene=<场景>`;mock 场景全集见 `dev/mock.js` 头注;
   - `dev/state-gallery.html`(http 打开,非 file://)全帧过目;**改到的帧必须重拍**;
   - 网络切换用 `GGMock._setNet(state, ssid?)` 全链真跑。
4. **契约纪律**:桥接契约 1.5.0 本轮不动;执行中若发现必须动契约(如 P1-11 深链的 `open_route`),停下单独拍板并 bump 版本,不顺手改。
5. **不顺手修**:只修本计划列出的问题;过程中发现新洞记到 G 章 backlog,不动手。
6. **复用硬约束(K 章)**:🅛 后端登录五态机 / 🔑 存库凭据登录服务 / 📄 登录页 / 📡 L1L2 探测 / 🔔 通知服务——**同标记=实现一份,禁止复制粘贴**。
7. **文案拟稿制**:标「拟稿」的文案执行时先落拟稿,全部完成后汇总一张表(H 章)请用户过目定稿。
8. **git**:每完成一项(或一组强相关小项)立即 `git add` + `git commit`,直接提交 master,信息格式 `fix(ui)/feat(ui): 条目号 一句话(计划 ui-interaction-rework v2)`。**Mimosa git 闸门已于 2026-09-10 由用户关闭**(此前拦截的 15 项高危为存量误报:SSRF=校园门户请求=产品功能、REJ_WRONG_PASSWORD=枚举常量,已拍板放行,见 commit 745bdad),本轮执行按本条正常提交。
9. **摆拍铁律**:`show()` 的 `.on` 有 250ms 延迟,**emit/断言必须延后 250ms**;pose 帧用独占 scene;画廊依赖全局函数入口(白名单见 D.3)。
10. **视觉不变原则**:P2 架构重构不得改变任何像素级渲染;重构前后 gallery 同帧截图对照。
11. **验收总门**见 F 章,全部通过才算闭环。

---

## A. 交互设计工程规范(宪法层——B/C 执行项的依据)

> A 章不是独立执行项,而是把审计中暴露的「洞为什么会长出来」固化成规则。每条 = 病灶(实测/代码定位)→ 规范 → 实现落点 → 验收。

### A1 反馈生命周期(main-lede 所有权)

- **病灶**:`main-lede` 有 4 个写入者(`renderMain` / `setAuto` / `quickLogin` 三处),无所有权约定,后写者胜。实测:点「立即重新登录」成功文案「刚刚把网帮你接回来啦 ✓」在 **82ms** 出现、**162ms** 被 `renderMainData→setAuto` 回写覆盖——成功反馈等效不存在。这正是 937b8a0 修过的「点击即切假成功」的残留变体:当时修了「提前切」,没修「切了被数据回填吃掉」。
- **规范**:lede 消息只分两类——
  1. **基线态**:由 `mainState` + 配置派生(「网的事交给我,你放心忙~」),唯一写入者是 `renderMain`/`setAuto`;
  2. **瞬时反馈(flash)**:动作结果的直接回话(成功/已在线/出错),默认 **4s**,后到覆盖先到,到期自动回基线。
  任何写入者在写基线前必须检查 flash 是否未过期;flash 未过期时**基线让位**。
- **实现**:新增 `flashMainLede(text, ms=4000)` 唯一入口:写 lede + 记 `S.flashUntil`(P2-3 落地前进全局变量)+ `setTimeout` 到期调 `renderMain()` 恢复基线;`renderMain` 与 `setAuto` 写 lede 前置守卫 `if(Date.now()<flashUntil) return`。`quickLogin` 的三处 lede 直写全部改走 `flashMainLede`。
- **验收(AC-1)**:「再登一次」成功 → 成功文案稳定显示 ≥4s;期间手推 `net:state` 不吞它;4s 后自动回基线;逐帧采样不再出现 <200ms 消失。

### A2 忙碌/防重矩阵

- **病灶**:防重现状一张纸糊——有 busy:`main-btn`/`login-btn`/v-ok 主按钮/`reProbe`/成功页重建/`fb-send`;**无 busy:`pickWifi`(SSID 点击可连点)、横幅②就地重建(只换文案)**。且 busy 窗口靠响应延迟撑着:`NOT_CONFIGURED` 路径 mock 零延迟返回,实测快速双击 login 调用 ×2——防重靠时序巧合,不是逻辑保证。
- **规范**:
  1. 凡 `await GG.api.*` 的点击入口必须过 `setBusy`;
  2. `setBusy` 增加 `minMs=400`:恢复时若实际 busy 时长不足则补足——**防重是时间下限保证,不赌网络延迟**;
  3. 视图转场期间(`show()` 的 250ms 窗口)旧视图 `pointer-events:none`(`.leaving` 类补这条),消灭「转场动画期连点穿页」。
- **实现**:`setBusy(btn,on,text,minMs)`;`pickWifi` 点击后给 `.ssid` 项加 busy 样式至 `connectWifi` 返回;`rebuildTaskBanner` 同理;`.v.leaving{pointer-events:none}`。
- **验收(AC-2)**:NOT_CONFIGURED 场景双击 `main-btn`,`GGMock.login` 调用计数 = 1;down 场景连点 SSID,`connectWifi` 调用 = 1。

### A3 事件打断策略(net:state 何时允许换页)

- **病灶**:统一路由(09-08 拍板「断网统一落 v-guide」)的副作用:**用户填表填到一半,unreachable 事件把人拽去排查页**。表单数据虽保留,但心流和「填到一半」的位置感已断——系统事件打断用户任务,交互反模式。
- **规范(拍板④细化)**:引入 `formDirty` 维度(表单合一后 = 学号或密码框任一非空):
  - `unreachable` + `formDirty` + 表单视图 → **不跳页**;状态行就地转 danger「10.1.2.3 · 连不上」(`syncLoginStatusLine` 已具备),提交按钮照常(保存配置语义本就允许断网提交);
  - `unreachable` + 空表单 / 终态页 / 详情页 → 维持统一跳 v-guide(09-08 拍板不动);
  - `waiting` → **任何视图都不跳**,按 B-P0-3 文案规范就地渲染(P0-3 是 A3 的第一个受益者);
  - **【v2 增补】系统调起状态页(P0-7/P1-8)同样受 `formDirty` 豁免**——填表中网恢复不拽人,状态行/横幅告知即可。
- **实现**:此规范在 P2-2 数据化时固化为路由表的一列;P0 阶段先在 `net:state` 处理器加 `formDirty` 早退分支(注明「P2-2 迁移」)。
- **验收(AC-3)**:v-form 填入学号后 `_setNet('unreachable')` → 停留表单、状态行 danger、可提交保存配置;清空表单后同样事件 → 跳排查页。

### A4 出口完备性(死胡同审计)

- **病灶**:拦截终态页唯一出口「重建定时任务」,失败无限循环,唯一逃生是标题栏 11px 小字(无视觉暗示可点)。
- **规范**:每个视图 ≥1 个**可见**出口;终态页 = 主出口(动作)+ 可选次出口(退出);自绘下拉补 **Escape 关闭**(外点关闭已有)。
- **实现**:拦截页加 quiet 次按钮(C-P1-1,拍板③);`document` keydown Escape → 收起 `.dd.open` 并把焦点还给 `.dd-btn`。
- **验收(AC-4)**:gallery 每帧可见出口 ≥1;dd 打开后按 Escape 收起、焦点回按钮。

### A5 焦点管理

- **病灶**:`show()` 换视图零焦点处理;键盘/读屏用户不知道页面换了,Tab 从头开始摸。
- **规范**:视图切换完成后把焦点移到新视图 h1(补 `tabindex="-1"`,CSS 去掉该聚焦 outline);控件补齐 aria 态:密码眼睛 `aria-pressed`、下拉按钮 `aria-expanded`。
- **实现**:`show(id)` 的 250ms 回调尾部 `$('…h1').focus()`(各视图 h1 统一加类 `.v-title` 便于选择);`togglePw` 同步 `aria-pressed`;dd 开合同步 `aria-expanded`。
- **验收(AC-5)**:切换视图后 `document.activeElement` 是目标视图 h1;读屏播报新页标题。

### A6 键盘可达

- **病灶**:43 个 inline `onclick` 里混着 `div onclick`(三条主页横幅、SSID 项)——键盘永远够不到「重建定时任务」「完整排查」这两个关键动作;自绘下拉无 Escape。
- **规范**:`div onclick` 的可点元素一律 `button` 化(浏览器默认样式重置回同形);dd 菜单项本就是 button(Tab 可达),补 Escape 即可。**明确不做**:完整 ARIA combobox roving-tabindex 模式——本产品 6 项静态菜单,过度设计。
- **实现**:横幅容器输出 `<button class="banner">`(样式微调去默认边框底色);SSID 项保持 div 但补 `role="button" tabindex="0"` + Enter/Space 键处理(列表项语义比按钮合适)。
- **验收(AC-6)**:Tab 遍历主页,三条横幅均聚焦可达、Enter 触发;SSID 列表键盘可选。

### A7 状态可见性(同屏信号矛盾消解)

- **病灶**:scene=ok 回主页同屏四信号打架——横幅①「密码还没验证过,去改一下」+ 最近结果「今早第一次就登好了 ✓」+ 网络行「已连上 ✓」+ 日志「已登录」。根因:`renderBanners` 只看 `rec.verified`,不看当前网态;契约 mock 的 `already` 路径 `verified` 不翻转 → **长期在线用户横幅①永挂**,每天被劝做最贵的动作(重输密码)。
- **规范(叙事分层)**:标题=现在的结论;状态行=证据;横幅=待办;最近结果=历史。同一屏不允许两条「现在」互相拆台。落地为一条判定规则:**横幅描述的「待办」必须与当前网态自洽——已在线时不存在「密码没验证」的待办**(验证需要把自己踢下线,不是待办是自残)。
- **实现**:`renderBanners` 横幅①条件追加 `&& !(lastNet && lastNet.state==='logged_in')`;横幅③文案口径见 C-P1-4。
- **验收(AC-7)**:scene=ok 主页无横幅①;scene=streak 主页标题(掉线)/状态行(还没登录)/最近结果(今早✓,历史)三者时态自洽。

### A8 动画时序单源

- **病灶**:转场 250ms 写了两份——JS `setTimeout 250` + CSS `.leaving{animation:fadeout .25s}`。改任何一边,另一边静默烂掉;而 state-gallery 的摆拍铁律(250ms 后 emit)又耦合在这个时序上——**测试基建和实现细节三重耦合**。
- **规范**:250ms 唯一源 = JS 常量 `VIEW_MS=250`;CSS 侧时长改读 CSS 变量 `var(--view-ms)`,由 JS 启动时 `setProperty` 注入。改一处,三处(JS/CSS/画廊约定)随动。
- **实现**:`const VIEW_MS=250` + `document.documentElement.style.setProperty('--view-ms', VIEW_MS+'ms')`;`.v{animation:fadein var(--view-ms)}…` 相应改写;`show()` 的 setTimeout 直接用 VIEW_MS。
- **验收(AC-8)**:grep 文件内不再有裸 `250`/`.25s` 时序魔法数(动画缓动曲线里的数值除外);把 VIEW_MS 改 500 目测转场同步无撕裂,改回。

---

## B. P0 行为正确性(用户路径上撒谎/吞反馈/基准失真/老用户被当新用户的洞)

### P0-1 flash 体系落地(挂靠 A1)
- 位置:`quickLogin`、`renderMain`、`setAuto`。
- 修法:按 A1 实现 `flashMainLede` + 基线守卫;`quickLogin` 三处直写改道。
- 验收:AC-1。

### P0-2 `already` 分支假成功
- 位置:`quickLogin` 的 `r.ok` 分支(现只判 `result==='stored'`,其余一律成功文案)。
- 修法:识别 `r.data.result==='already'` → `flashMainLede('你已经在网上了,桂桂没动')(拟稿)` + `mood(gb,'idle')`,**不冒领**;`mainState` 不动。
- 边界:`enableDaily`/`loginNow` 的 already 路径(已在线首装 → 「保存配置」终态)**语义正确,不动**。
- 验收:scene=ok 主页点「立即重新登录」→ 文案如实;不再出现「把网接回来」。

### P0-3 waiting 态处理(拍板⑤改写:全局删除开门判断)
- 原方案(waiting 文案贯穿/排查页感知)**作废**;等门页 bootWait **删除**,「校园网 06:50 才开门」系文案全部下线。
- 新行为:`net:state` 的 `waiting` 一律视同 `unreachable`——唯一落点 v-guide;`netText`/状态行/首装分流同步两态化(可达/不可达)。
- 后端不动:detect 四态保留,前端映射层压平(waiting→down)。
- 验收:mock `_setNet('waiting')` 打在任意视图 → 与 unreachable 行为完全一致;bootWait 函数与等门帧删除,gallery 对应帧移除。

### P0-4 `NOT_CONFIGURED` 静默跳转 + 防重(挂靠 A2)
- 位置:`quickLogin` 的 `NOT_CONFIGURED` 分支(裸 `openDetail('v-login')`,零解释)。
- 修法:跳转前 `showLoginErr('还没存过密码,先填一次')(拟稿)`;配合 A2 的 `minMs` 堵零延迟路径。
- 验收:AC-2 双击样本;落地登录页带警告块解释。

### P0-5 横幅①条件收口(挂靠 A7;条件 09-09 二轮补验后修正)
- 位置:`renderBanners`。
- 修法:横幅①条件改为 **`verified===false && (lastNet && lastNet.state!=='logged_in' || 最近结果 outcome==='fail')`**。
  ⚠️ 初版守卫「已在线即隐藏」太粗,二轮补验 scene=unverified(已在线+未验证+**今早真失败**)时被否——这时密码大概率真错,横幅①是有信息的,一刀切会错杀。精确语义:**「未验证且(不在网 或 最近失败过)」才显示**。
- 【v2 增补】横幅①点击行为 = 去 📄 登录页(白板 vb1→登录页);横幅②=拦截态(master 开关联动)、③=反馈相关(文案拟稿)——语义注记见白板图例,P1-13 落地。
- 验收:AC-7;补 scene=unverified → 横幅①**在场**;scene=ok → 横幅①**不在场**。

### P0-6 mock 契约对齐:「线上他人」已存凭据路径(09-09 二轮补验实锤)
- 病灶:mock 与真后端在「线上是他人学号」的**已存凭据重登**路径上行为相反——
  真后端([api.py:189-199](guigui/app/api.py#L189)):`logged_in` 时先 `chkstatus_uid` 核对,线上是他人 → **不报 already,改走真登录**(注释原文「别人的成功不冒领」);
  mock([mock.js:146](guigui/app/static/dev/mock.js#L146)):`logged_in` 一律 `return OK({result:'already'})`,阶梯只在带 password 时走。
  后果:前端在 mock 的 other/ladder_back 场景练的「主页重登 = already」在真机不存在;mock 自称「可执行规范」却停在旧版,后续所有前端验收都建立在错误基准上。
- 修法:mock 的 `login` 无 password 分支对齐后端——other/ladder_back 场景 `logged_in` 时模拟 chkstatus 他人 → 走「注销他人 → 真登」阶梯(发 `login:progress` 事件,返回 success+attempts=1);线上是自己 → already(现状)。P0-2 的 already 文案修复不受影响。
- 纪律:这是把 mock 对齐既有后端行为,**契约文档不动、后端 0 行改动**;mock 头注场景说明同步更新;state-gallery 依赖旧 already 行为的帧同步重拍。
- 验收:scene=other 主页「立即重新登录」→ 等待行出现注销告知 → 真登成功;scene=daily(自己在线)→ already 文案如实。

### P0-7 【v2 改写·拍板⑪/S3】网恢复 = 调起状态页 + 快登入口(不自动重登)
- 病灶(v1 实测,保留记录):已配置用户断网后网恢复——排查页路径被拉去**空密码表单**(实测 `f-pwd-login` 长度 0);保存配置页路径**完全静默**;该页文案承诺「连上校园网后,桂桂自动执行登录」——GUI 非常驻、GUI 自己不探网,承诺的唯一执行者是明早任务。**系统明知用户有凭据,却把老用户当新用户。**
- 【v2 修法】:**不做后台自动重登**(拍板:只亮状态,让用户动手;自动动作仅限未验证库的网恢复验证)。网恢复(GUI 在场)→ 📡 探测 L2 判未登录 → **系统调起大头状态页**(P1-10)亮状态,「快速登录」按钮在场(P1-12)一键登;`formDirty` 豁免(A3);未配置 → 现状表单路径不变;生效视图:v-guide / v-success(保存页)/ v-main 三处统一此分岔。
- v-login/v-guide「连上后自动登录」承诺文案**下线/改写**(H 拟稿)——不删就是继续撒谎。
- 契约纪律:0 行 Python;探测复用 P1-8 机制,快登复用 🔑 服务。
- 验收:down 场景 ③存配置 → 保存页 → `_setNet('not_logged_in')` → **调起状态页**(非空表单、非自动登);configured 排查页网恢复 → 状态页;点「快速登录」→ 真登成功 → 日常页·全部正常;填表中网恢复 → 不跳页(A3 优先)。

### P0-8 (已作废,拍板⑤取代)
- 原「首装+waiting 专属等门」随开门判断全局删除而作废:首装 waiting 与 unreachable 同路落 v-guide,不建新等门页。保留编号占位防错引。

---

## C. P1 体验补全与白板定稿落(v2 补全 P1-1~P1-7 正文;P1-8/10 改写;P1-12~17 新增)

### P1-1 拦截页双出口(拍板③;挂靠 A4)
- 主按钮「重建定时任务」保留;加 quiet 次按钮「先不管,回主页」(拟稿)→ 返回日常页(横幅②在场)。
- 【v2 白板对齐】拦截页出口两向:重建成功 → 回任务链(白板「加白后重建成功·回任务链」);重建 3 次失败 → 问题反馈(附诊断包)。
- 验收:拦截页两按钮可见可用;AC-4。

### P1-2 触发时间前缀动态化 + 改时间静默对齐
- 病灶:设置页时间前缀「明早」写死,傍晚配置显示「明早 07:00」对当天晚上任务是谎话。
- 修法:按当前时刻派生 明早/今天/今晚(H 拟稿)。
- 【v2 白板对齐】改时间保存 → **静默对齐定时任务(不触发登录)**(白板「改时间→静默对齐定时任务」节点):保存后任务静默改期,不弹任何额外页面、不发起登录;对齐完成回日常页。
- 验收:daily 场景改时间,前缀随时刻变化;改期后任务触发时间变更且**无登录调用**;gallery 帧同步。

### P1-3 排查页 SSID 当前标记
- 挑网列表当前已连项加「当前 · 连着」标记(拟稿),消除「我连的到底是哪个」的疑惑。
- 【v2 白板对齐】选 WiFi 后 → 回「网络可达?」重探(白板 v-guide→pan1),不是直跳登录态判断。
- 验收:down 场景列表首项带标记;挑网后走可达性重探。

### P1-4 横幅③文案 + 计数口径
- 文案见 H 表(「反复登录都被拒?完整排查」拟稿);真历史计数依赖 `fail_streak` 信封字段(契约 1.6.0,G 章 backlog,本轮用内存计数过渡且文案不出现具体次数)。

### P1-5 可达性文案包 + bot aria 桂桂口吻
- vendor bot 的 aria-label 在 `initBot` 统一覆写为桂桂口吻(H 表),修「小匠」泄漏;可达性相关文案(netText/状态行/等待行)口径统一收口为一个文案包函数。

### P1-6 体检过期提示
- 诊断页断网返回后旧结果灰化 + 「网络刚变过,结果可能过期,重跑一次」(拟稿)提示与重跑入口。

### P1-7 拦截页标题口径(验证器路径)
- 验证器任务失败进入拦截页时,标题「密码存好了,自动登录还差一步。」错位(密码可能没问题,是任务被拦)→ 改「密码没问题,自动登录还差一步。」(拟稿)。

### P1-8 【v2 改写·拍板⑪】探测机制:L1⇒L2 后台常驻(全局一份)
- 机制:**L1 连通性(每 5s → 10.1.2.3,纯连通性,数字写死)⇒ L2 登录态(chkstatus)**;后台常驻——日常页「全部正常」时也在跑;**唯一显示面板 = 大头状态页**(每 5s 刷新状态行);**系统调起去重**:同一问题事件只调起一次状态页,后续失败只更新显示;巡检「网」维度 = 读探测结果,不另查。
- 定时器随 GUI 生命周期;休眠/最小化暂停为工程注记(G 章),不阻塞本项。
- 【v1 口径作废记录】原「5s 探测挂状态页(仅页面在时)」升级为全局常驻。
- 验收:mock 断/通切换,状态页 5s 内自动迁移;全部正常态下探测仍在跑(可观测计数);无泄漏定时器;调起去重(断网期间只调起一次)。

### P1-9 后端缺席页加重试(09-09 三轮入册,D5)
- 病灶:`BRIDGE_MISSING` → 仪式页文字劝「重启桂桂;反复出现请重装」,无重试按钮——A4 出口完备性漏了仪式页。
- 修法:boot-cap 下加 quiet 按钮「再试一次(拟稿)」→ 重新走 firstRun 探测;`GG.ready`/dispatch 机制现成支持。
- 验收:dev 下断 mock 注入模拟 BRIDGE_MISSING → 点重试 → 恢复后正常分流;连续失败文案如实。

### P1-10 【v2 改写·拍板⑪】大头状态页 = 问题中枢(三心境合一)
- 定义:**非首装独有**的全屏问题态页面,复用成功页舞台(大头 bot);三种入场心境由**入场原因**决定配色与文案——蓝变红⑧(网在,登录态丢了)/ 日志矛盾(今早没登上,当下都正常)/ 沮丧⑦(坏消息:凭据被拒)。与主页横幅**并存**。
- 功能专一(拍板原话):当前问题 + 实时状态(每 5s,L1/L2)+ **快速登录按钮** + 两选项(去登录页改 / 换账号?)——无冗余信息。
- 入口表(白板口径):横幅⑧点击 / 巡检·日志实况分诊①②③ / L2 未登录·系统调起 / L1 还不通·系统调起(等网)/ 快登或检测被拒(沮丧)。
- 出口表:点「快速登录」(P1-12)/ 「进入检测」(**仅矛盾态显示**,跑 🔑 检验)/ 去登录页改 / 换账号?(换→登录页;不换→带横幅回日常)/ 带横幅回日常 / 网侧排查(→v-guide)。
- 按钮显示判断:两选项**互斥**——凭据类(密码/运营商错)亮「去登录页改」;绑定/别设备亮「换账号?」;由 🅛 后端返回分支决定。
- 验收:gallery 状态页三心境帧;五入口各自落点正确;出口表逐条可走;返回后横幅在场。

### P1-11 通知深链(契约 §6 backlog 提级;依赖 J 章通知策略)
- 通知点击 → 打开 GUI 并直达对应视图:拦截页 / 登录页 / 大头状态页 / 问题反馈。
- 契约侧:新增启动路由参数(`open_route`),后端启动时透传给前端路由;契约版本 bump(执行时与后端计划对齐,单独拍板)。
- 验收:mock 注入通知路由 → 打开落点正确;无路由参数时行为与现状完全一致。

### P1-12 【v2 新增·拍板⑪】手动快登(状态页「快速登录」)
- 流程:状态页点「快速登录」→ 🔑 存库凭据登录(自适应)→ 🅛 后端返回五态——
  成功 → 日常页·全部正常(状态页不弹彩带,彩带规则见 P1-16);
  节流 → 中性等待提示,留在状态页可再试(不算凭据坏);
  密码/学号运营商错 → 沮丧心境 + 亮「去登录页改」;
  绑定/别设备 → 沮丧 + 亮「换账号?」。
- 防重挂 A2;这是「用户从外面回来想登一下」的主路径(拍板:快登入口在状态页,不在日常页)。
- 验收:mock 四拒+节流+成功全场景从状态页走通;busy 防重;成功后日常页两态正确切换。

### P1-13 【v2 新增·拍板⑪】日常页两态化 + 横幅②③ + 总开关
- 日常页**仅两态**:①全部正常(无横幅)②带横幅(从状态页/故障页返回自带);状态事实(网在/登录态)由状态行+📡探测呈现,不再是独立分支链。
- 横幅语义定稿(白板图例口径):①未验证/验证失败提醒(点击→📄登录页);②拦截态(master 开关联动);③反馈相关(文案拟稿 H 表)。
- 总开关(停自动登录):任务停用/拦截解除/横幅②联动——设置页交互,操作流不在白板(图例注记),实现按本条。
- 验收:gallery 两态帧;横幅①②③显示条件各自自洽;总开关切换后任务态与横幅②联动正确。

### P1-14 【v2 新增·拍板⑫】检验自适应服务(🔑,实现一份)
- 语义:**未登录 → 真登;已登录 → chkstatus 对账**(会话账号=存库学号即通过);**禁注销全量**(共享会话/断网风险,校园会话记忆条在册);对账验不了密码的盲区由下一未登录时刻(掉线/明早任务/网恢复)自然暴露,走五态分流。
- 使用方:状态页「快速登录」/ 矛盾态「进入检测」/ 未验证库验证——**同一实现,K 章硬约束**。
- 触发器三入口(白板 vt1/vt2 口径):**明早 07:00 定时任务首试**(vt1)/ **网恢复且库中有未验证凭据**(vt2;此为唯一保留的自动验证动作,日常登录侧不自动,见 P0-7)/ 矛盾态·进入检测;vt2 边注「登录态探测[有问题]触发」的精确口径执行时与后端核对。
- 契约:复用既有 stored-credential login 语义(api.py 189-199 已是这个逻辑),0 行后端改动起步;若 envelope 需区分「对账通过/真登通过」再单独拍板。
- 验收:mock 三入口走同一函数(grep 无第二实现);已登录场景点检测=对账路径不触发登录;未登录场景=真登;vt2 触发仅在「未验证库非空 ∧ 网恢复」时发生。

### P1-15 【v2 新增·拍板⑬】故障带自愈四链(后端为主,前端横幅/页面)
- 巡检本体:五维 = 网(=📡 探测结果)/ 任务 / 日志 / 程序 / 库;周期与触发(启动+事件)执行时与后端对齐,前端只消费结果信封。
- **库**:损坏/解密失败 → 自动重建×3 → 成功回巡检复查;3 败 → 降级=切备份库 JSON → 成功回巡检;备份也坏 → 问题反馈;全程 🔔(J 章)。
- **任务**:被删/被禁/被杀软拦 → 拦截页重建直到正常 → 成功回巡检;3 败 → 问题反馈。
- **日志**:今早没登上 → ◇当日实况分诊(调 📡 探测)→ ①全好=矛盾 → 状态页(显示「进入检测」)②没登录 → 状态页 ③网不通 → 状态页(等网)。
- **程序**:文件损坏/被杀软隔离 → 自动修复(自校验还原)→ 成功回巡检;失败 → 🔔 反馈附重装指引。
- 自愈成功统一「回巡检复查」(确认真好了再回日常),不直接回日常页。
- 验收:mock 逐维触发→自愈→复查链走通;🔔 场景与 J 章表逐行对账;巡检信封字段若不足,列契约缺口单独拍板。

### P1-16 【v2 新增·拍板⑯】成功页矩阵(两个成功页)
- **彩带页(既有 okpg)**:触发 = **用户在场的验证通过**——首装链通过 + 日常改密验证通过(GUI 低频,在场时刻都值得庆祝);出口 = 高级设置 / 回主页(**按当前状态**);明早无人值守成功**不弹彩带**(通知+日常页状态)。
- **保存配置成功页(新增 okpg2)**:触发 = 配置模式(存凭据+建任务)建成;文案「保存配置成功」;返回 = **不可达排查页**(按当前状态——配置模式发生在断网语境)。
- 验收:gallery 两成功页帧;彩带触发条件(在场/无人值守)分支正确;okpg2 返回落 v-guide。

### P1-17 【v2 新增·拍板⑮】任务①②生命周期
- 「任务① 建成?+通知」/「任务② 登上网?」(白板已从「验证器①②」改名,与 park 的验证器脱钩):
  创建失败(杀软拦?)→ 拦截页;
  登录失败 → 🔔 带原因(含连不上)→ **🅛 五态分流(复用,拍板 B)**——明早 7 点任务跑 = 进非首装链路,成败落日志/巡检;
  任务完成·删任务 → 🔔 成功通知(默认开,J 章)。
- 验收:mock 任务链三结局(建成+登上/建成+登录失败/建成失败)各自落点正确;登录失败不进拦截页。

---

## D. P2 架构收口(3 项,拍板②;视觉零变化)

### D.1 P2-1 表单合一
- **范围**:`v-ok` + `v-login` → 单视图 `v-form`;`enableDaily` + `loginNow` 两条验证阶梯(throttled / AUTH_REJECTED / NET_UNREACHABLE / already / 兜底,各写一遍)合并为 `submitLadder(ctx)`,`ctx ∈ {firstRun, login, save}`(v-guide ③)。
- **参数化差异**:标题(早上好/登录校园网)、lede、按钮文案(开启每日自动登录/立即登录/保存配置)、警告块 id、状态行来源、`formDirty` 判定——全部由 ctx 派生,一套 DOM。
- 【v2 对齐】本项与 📄 登录页共用口径(白板已合并首装/日常登录页为单节点):首装与日常共用一套 DOM,返回键=来路栈(从状态页来回状态页,换账号/深链回日常页)。
- **执行纪律**:
  1. 动手前把两页现渲染截图存档,合并后逐像素对照(视觉不变原则);
  2. `ok-note`/`login-note` 合并为 `#form-note` —— **state-gallery 的 `notePose` 帧引用这两个 id,必须同步改**(见 E 章);
  3. 双份运营商下拉(`#op-dd`/`#op-dd-login`)随合一自然消失,`pickedOperator`/`setOperatorPick` 去掉 selector 参数。
- **验收**:gallery 的 v-ok/v-login 全部故事帧重拍通过;三条提交路径(首装/登录/保存配置)mock 全场景回归。

### D.2 P2-2 路由总表数据化
- **范围**:`net:state` 处理器 28 行 if 链 + 3 处重复的 `mainState` 三元压缩 → 表驱动:
  - `netToMain(state)`:四态 → ok/out/down 的**唯一**压缩点(waiting 列按 B-P0-3 规范落文案,不再裸压 down);
  - `ROUTE[view][netState] = {stay|goto|render}` 查表,含 A3 的 `formDirty` 豁免维度与【v2】系统调起状态页的去重维度;
  - `show`/`openDetail`/`backFrom` 签名不变(画廊依赖)。
- **收益断言**:新增一个网态或一个视图 = 改表一行,不再人肉改 4-6 处。
- **验收**:gallery 全故事行为等价重跑;矩阵文档错误路由总表与代码表逐格对得上(E 章)。

### D.3 P2-3 store 收口
- **范围**:13 个散全局(`mainState/configured/hasPwd/lastNet/curCfg/taskBlocked/loginFailStreak/successState/fbKind/fbDraft/diagState/currentTriggerTime` + A1 新增 `flashUntil`)→ 单一 `S` 对象;函数**保持全局**(画廊白名单依赖),但状态读写一律走 `S.x`。
- **验收**:`grep -nE '^(let|const) ' index.html` 的 JS 段仅剩 `S`、常量(`VIEW_MS/MAIN_LOG_KEEP/DIAG_KEYS` 等)与函数;`bots` 映射可保留。
- **画廊兼容白名单**(重构中不许破坏,每改一处同步核对该清单):
  - 函数:`show` `openDetail` `backFrom` `mood` `fbPickKind` `greet` `renderGuide` `renderMain` `renderMainData` `fillLoginLanding` `loadFeedback` `loadSettings` `loadLogs` `startDiag` `win` `minimize` `closeWin`;
  - id:`boot-bot` `boot-cap` `main-h1` `main-lede` `main-btn` `main-banners` `main-log-entries` `diag-*` `fb-*` `success-*`(`ok-note`/`login-note` 随 D.1 迁移为 `form-note`,画廊同步);
  - 时序:`VIEW_MS`(A8)——画廊摆拍铁律跟着这一个常量走。

---

## E. state-gallery 重拍清单 + 矩阵/白板同步义务

- **必重拍帧**:v-ok 全帧、v-login 全帧(D.1 表单合一);排查页帧 + **新增 waiting 态排查帧**(B-P0-3);拦截页帧(C-P1-1 两按钮);主页横幅帧(①新显示条件、③新文案);「发送中」「必填空」帧(id 迁移);**新增体检过期帧**(P1-6);**other/ladder_back 主页重登帧**(P0-6)。
- **【v2 新增帧】**:大头状态页三心境(蓝变红⑧/日志矛盾/沮丧⑦,P1-10);日常页两态(P1-13);状态页快登流·五态落点(P1-12);保存配置成功页(P1-16);故障横幅②+总开关联动(P1-13);任务①②链三结局(P1-17);网恢复调起状态页(P0-7)。
- **新增故事**:「填表中收到断网:不跳页,状态行转 danger」(A3/AC-3);【v2】「全部正常态探测仍在跑」(P1-8 观测帧);【v2】「故障自愈→回巡检复查」(P1-15)。
- **矩阵文档**(`docs/prd/guigui-state-matrix.md`):
  1. 错误路由总表补 waiting 行(P0-3);
  2. 填表豁免例外注记(A3,拍板④);
  3. 横幅①显示条件更新(P0-5)+ ②③语义(P1-13);
  4. 拦截页出口更新(P1-1);
  5. `v-ok`/`v-login` → `v-form` 的帧结构变更(P2-1);
  6. 【v2】状态页入口/出口表(P1-10)、快登五态落点(P1-12)、两成功页(P1-16)入矩阵。
- **交互路由全景图**(`docs/prd/flows/guigui-frontend-routing.workflow.json`):as-is/to-be 双焦点视图;每修完一项把图上对应「拟」节点/边转正。
- **【v2】白板同步义务**:每完成一项 v2 条目,核对 `guigui-user-flow.dsl.json` 对应节点/边是否需转正或加注(白板=正本,代码实现后白板的「拟」性描述应转为实现事实);推送走 flows 目录推送配方(四层 z)。

## F. 验收总门(全部通过才闭环)

1. **审计对账**:2026-09-09 审计 10 洞逐条对账关闭(P0-1~5 → 洞 1/2/3/4/8/9;P1-1~5 → 洞 5/6/7/9/10;A 章规范挂靠齐全)。
2. **mock 全场景**:内置浏览器全部 scene 跑一遍,无新洞、无回归(重点:already 假成功、waiting 贯穿、填表豁免、双击防重)。
2a. **事件路由格全走查**:`net:state` 四态 × 主要视图逐格 `_setNet` 验证落点(P2-2 落地后按 ROUTE 表逐格对);未代验格(v-log / v-feedback / v-success 收 unreachable;各视图收 waiting)执行时补。
3. **gallery 全帧**过目 + 必重拍帧更新(含 v2 新增帧);矩阵文档同步完成。
4. **双击复测样本**:NOT_CONFIGURED 场景 login 调用 = 1;成功 flash ≥4s(AC-1/AC-2 数据化复测)。
5. **读屏抽查**:bot aria 全部桂桂口吻;横幅 Tab 可达。
6. **文案定稿**:H 章拟稿表请用户过目,定稿后统一替换再提交。
7. **【v2】白板对账**:代码实现后对照 `guigui-user-flow.dsl.json`(69 节点/107 边)逐簇走查——每个页面节点的入口/出口在实现里都存在且落点一致;不符处要么改代码要么改白板(走配方),不允许两边漂移。
8. **【v2】复用对账**:grep 确认 🅛 五态机/🔑 检验/📄 登录页/📡 探测/🔔 通知各只有一份实现(K 章);通知场景与 J 章表逐行对账(含冷却:同类 30 分钟二连失败只见一条)。

## G. Backlog(本轮不做,发现新洞也记这里)

- **D4|identify 误抓的归因误导(待用户拍板)**:首装在线回填他人学号(「已识别✓」背书)→ 阶梯注销他人 → 被拒「密码不对」→ 用户归因自己密码死循环;可选缓解:首装被拒 ≥2 次时警告块附「也核对下学号是不是你自己的」——是否采纳由用户定。
- **E park|验证器专用计划任务机制(拍板⑨先不管)**:「创建一次性验证任务→验完删除」的端到端机制,涉及契约新方法与 schtasks 拦截误报风险,本轮不做;后端 diagnose 保持不动。
- **D6|改密路径 lede 文案错位(待用户拍板,涉用户手写文案)**:设置→账号修改 → v-login 的 lede「还没登录,填一次…」是首装语境;该句为用户亲手改定,按来路(origin)换文案需用户点头。
- `recentResult` 信封加 `fail_streak`(横幅③真历史计数)——契约 1.6.0,需后端配合;
- 主文件拆分(css/js 出去)——等 store 收口后重新评估;
- 时间输入 ↑↓ 微调;完整 combobox 键盘模式(已判过度设计);
- 时间钳制 err 态延迟落盘(现行为所见即所存,保留);
- 【v2】L1 探测工程注记:GUI 休眠/最小化时暂停;系统调起状态页去重窗口;长时间等网的退避(5s 写死为拍板,退避仅作工程优化不改变文案承诺);
- 【v2】**契约 1.6.0 缺口清单(P1-15 执行时列出,2026-09-11,单独拍板;拍板前契约保持 1.5.0)**:
  1. **巡检程序/库两维的只读信封方法**(如 `inspect()` 或 probe 扩展)——现契约面只有 diagnose(有副作用,铁律禁启动跑)可查程序维(crashlog),库维完全无可见性;
  2. **自愈进度/降级状态信封字段**——库降级横幅(白板 degrade「🔔降级:切备份库JSON」+ J 章「凭证库降级·备份接管成功→日常页横幅在场」)的数据源;
  3. 依赖上述两件的**库链**(备份库 JSON 的加密与安全设计、自动重建×3、降级切换、降级🔔)与**程序链「自动修复(自校验还原)」**(打包形态 exe 损坏进程不起,需先拍板「自动修复」的现实边界 — 数据文件损坏已有兜底,exe 侧现实解=BRIDGE_MISSING 仪式页重试+重装指引);
  4. **通知侧缺口**(J 章表对账,涉 ensure/notify 语义重写):例行任务成功默认开·可关 / 任务失败·连不上纯诊断也发(**与假期静默拍板(2026-09-06 PRD)存在语义冲突,需用户裁决**)/ 完整冷却(同类 30 分钟合并、每类每天≤1 — 现为「每日≤1」粒度);
  5. P1-11 深链 `open_route`(契约 §6 既有票,与本清单合并一并拍板 bump)。

## H. 文案拟稿汇总(执行完统一请用户过目)

| 位置 | 现文案 | 拟稿 |
|---|---|---|
| P0-2 already 反馈 | 刚刚把网帮你接回来啦 ✓ | 你已经在网上了,桂桂没动 |
| P0-4 NOT_CONFIGURED | (无) | 还没存过密码,先填一次 |
| P0-3 waiting 主页标题 | 现在连不上校园网。 | 网络还没就绪。 |
| P0-3 waiting 主页 lede | 现在够不着校园网,我陪你查查看。 | 可能刚开机,还没到点,桂桂在等开门。 |
| P0-3 waiting 排查页① | ① 当前连的是「X」,像热点。挑一个校园网: | 可能刚开机或还没到点。校园网 06:50 开门,桂桂在等: |
| P0-3 reProbe waiting | 还是连不上,再试一次 | 门还没开,再等等 |
| P0-3 netText waiting | X · 连不上 | 网络还没就绪 |
| P1-1 拦截页次按钮 | (无) | 先不管,回主页 |
| P1-2 时间前缀 | 明早(写死) | 明早/今天/今晚(按时刻) |
| P1-3 SSID 当前标记 | (无) | 当前 · 连着 |
| P1-4 横幅③ | 试了几次还没登上?完整排查 | 反复登录都被拒?完整排查 |
| P1-6 体检过期提示 | (无) | 网络刚变过,结果可能过期,重跑一次 |
| P1-7 拦截页标题(验证器路径) | 密码存好了,自动登录还差一步。 | 密码没问题,自动登录还差一步。 |
| P1-9 缺席页重试按钮 | (无) | 再试一次 |
| P1-5 bot aria | 小匠文档助手,待命 等 | 桂桂在待命/桂桂在想事/桂桂很开心/桂桂有点急/桂桂睡着了 |
| P1-10 换账号提示 | (无) | 这个账号在别处登着,换一个账号试试? |
| ~~P0-3 等门系文案~~ | 06:50 才开门,桂桂在门口等着… 等 | 随开门判断全局删除一并在 UI 下线(拍板⑤)**✅ 已下线(P0-3 执行)** |
| 【批1新增】P0-3 stored 反馈 | 还没开门,密码先存着,开门自动登 | 时间还没到,密码先存着,到点自动登 |
| 【批1新增】P0-3 锚前保存配置 lede | 校园网 06:50 开门后,桂桂自动验证登录。 | 时间还没到,桂桂先把密码收好,到点自动验证。 |
| 【批1新增】P0-3 boot 可达回日常提示 | 门开了,桂桂进去了… | 网络好了,桂桂来了… |
| 【v2】P0-7 承诺文案改写 | 连上校园网后,桂桂自动执行登录 | 连上后一键就登,桂桂把按钮递到手边 **✅ 已落(P0-7 执行)**:保存配置页 lede 逐字落地;v-guide ③ 同步改「③ 先保存配置,连上后一键就登」 |
| 【v2】P1-12 快登按钮 | (无) | 快速登录 **✅ 已落(P1-12 执行)** |
| 【v2】P1-12 节流反馈 | (无) | 服务器让等 N 秒,稍后再试 **✅ 改落(P1-12 执行)**:实际走后端 rejection_text 原话直出(mock 逐字一致),前端不另拟 |
| 【v2】P1-16 保存配置成功页 | 配置保存成功! | 保存配置成功,明早 07:00 见(拟稿,时间动态) **✅ 已落(P1-16 执行)**:落为不可达语境 okpg2 标题「保存配置成功,今天/明早 HH:MM 见。」(triggerPrefix+currentTriggerTime 动态);锚前/降级变体保持「配置已保存。」不动 |
| 【批3新增】P1-16 okpg2 返回按钮 | (无) | 回排查页(拟稿;配置模式唯一出口,白板 okpg2→vguide「返回(当前不可达)」) **✅ 已落(P1-16 执行)** |
| 【v2】🔔 通知组(vok/vfail/拦截/修复失败/降级) | (无) | 今早自动登录成功 ✓ / 今早没登上:密码不对,去改一下 / 杀毒软件拦了桂桂,去加个白 / 程序文件坏了,重装指引在这 / 凭证库切到备份了,建议重新存一次(全部拟稿) |
| 【批3新增】P1-10 状态页标题(四境) | (无) | 还在等网络通。/ 网在,登录掉了。/ 今早没登上,但现在都正常。/ 坏消息:登录被拒了。(拟稿) |
| 【批3新增】P1-10 状态页 lede | (无) | wait:10.1.2.3 还够不着 — 连上校园网,桂桂马上接着办。/ drop:校园网通了,但登录掉了,点一下就回来。/ contra:日志和现状对不上,再检测一次看看。/ sad:后端拒绝 message 原话直出(同表单页警告块口径)(拟稿) |
| 【批3新增】横幅⑧ 两案(P1-10 手动入口) | (无) | 网络还没通,桂桂在等 — 点开看看 / 网在,还没登录 — 点一下就回来(拟稿) |
| 【批3新增】状态页按钮组 | (无) | 快速登录 / 去排查网络 / 进入检测 / 去登录页改 / 换账号? / 换一个账号 / 不换,回主页 / 带横幅回日常(拟稿) |
| 【批3新增】状态页实时状态行三态(P1-8 探测喂) | (无) | X · 已登录 ✓ / 10.1.2.3 · 已连通 · 还没登录 / 10.1.2.3 · 还不通(拟稿) |
| 【批3新增】P1-12 快登成功 flash | (无) | 登好了,网的事继续交给我 ✓(拟稿;stored 变体复用批1「时间还没到,密码先存着,到点自动登」)**✅ 已落(P1-12 执行)** |
| 【批3新增】P1-14 进入检测 busy 文案 | (无) | 检测中…(拟稿;contra 心境的按钮等待态,区别于快登「登录中…」)**✅ 已落(P1-14 执行)** |
| 【批3新增】P1-15 拦截页 3 败按钮 | 没建成,再试一次 | 3 次没建成,带上诊断包反馈(拟稿;重建 3 败后主按钮换此文案,点击=复位计数+重渲+跳问题反馈附诊断包,白板 re3;返回拦截页时「重建定时任务」已恢复)**✅ 已落(P1-15 执行)** |
| 【批3新增】P1-15 横幅② 3 败文案 | 没建成,再试一次 | 重建 3 次没成 — 带上诊断包反馈(拟稿;横幅②三连败换此文案,点击=复位+跳反馈页,返回后横幅回「点此重建」)**✅ 已落(P1-15 执行)** |

---

## I. 补验记录(2026-09-09 第二轮,交互链全走查)

> 用户质询「交互链你真的全部验证过吗」后补跑。以下为**已实测走通**的链(执行时作回归基线);标注⚠的为本轮新发现、已入计划。

| 链 | 场景 | 结果 |
|---|---|---|
| 首装在线+识别回填+空密码警告+保存终态 | ok | ✓(一轮) |
| 首装断网→排查页→挑网→连上→登录 | down | ✓ |
| 主页重登成功 | daily | ✓ |
| 主页重登 already(mock 旧基准) | other | ✓⚠P0-6(mock/后端偏离实锤) |
| 阶梯翻车两幕:他人学号回填→注销他人→被拒「网先断着」→改对成功 | ladder_fail | ✓ |
| 阶梯回滚:改密被拒「已用旧密码接回来」→网恢复 | ladder_back | ✓ |
| 节流:等待行中性文案,不进密码警告 | throttled | ✓ |
| bind 被拒:自助平台六字内联链接渲染 | bind | ✓ |
| limit 被拒:教育文案 | limit | ✓ |
| 锚前存入:重登如实「还没开门,密码先存着」 | beforeopen | ✓ |
| 未验证+今早失败:横幅①在场(显示条件需精化) | unverified | ✓⚠P0-5 修正 |
| 验证器任务败:exit=task_blocked→拦截页 | diag_task | ✓⚠P1-7 标题口径 |
| 验证器程序败:出口按钮隐藏+就地结论 | diag_app | ✓ |
| 反馈降级:submitted_degraded 按「收到」处理 | fbdg | ✓ |
| master 开关联动:拦截态横幅②/关闭后消失 | blocked | ✓(「关闭后残留」经查后端是幽灵任务故意告警,非洞) |
| 体检中断网:拽排查页正确;返回后旧结果过期 | daily+diag | ✓⚠P1-6 |
| 挑网自动路由:guide→连网→v-login | down | ✓(一轮) |

**未代验、执行时必须补**:v-log/v-feedback/v-success 收 unreachable 的落点;各视图收 waiting(除已验三格);bind 内联链接点击后新标签页打开(openSelfService);applyLaunch 深链(creds/settings);`log:appended` 在 v-log 页的追加。

**三轮补验(2026-09-09,链级设计评估)**:D1 排查页(configured)网恢复 → 实测落 v-login 且密码框空;D1b 保存配置页网恢复 → 实测完全静默;D7 GUI 侧无自轮询 → 代码查证(net:state 全部翻译自 ensure 落盘)。46 条链全景评估结论:21 健康 / 12 bug 级(已入册)/ 7 链级设计问题(D1-D7,其中 D1+D2→P0-7v1、D3→P0-3、D7→P1-8、D5→P1-9;D4/D6 待拍板在 G 章)/ 6 待验待裁决。**【v2 注】P0-7 已按拍板⑪改写,上表 D1/D1b 的病灶描述保留作历史记录,修法以 v2 为准。**

---

## J. 通知策略(2026-09-10 与用户拍板;白板 🔔 标记同源)

原则:**通知是唯一常驻感知面(GUI 不托盘)**——只在"需要你知道或动手"时开口;成功通知默认开(用户拍板,设置可关)。触发一律在**重试穷尽后**(过程静默,报结果)。

| 事件 | 通知 | 深链落点 |
|---|---|---|
| 例行任务成功(任务完成·删任务) | ✅ 默认开·可关 | 无(纯告知) |
| 任务最终失败·凭据类(密码/学号运营商/绑定/别设备) | ✅ 带原因+下一步 | 登录页/大头状态页 |
| 任务最终失败·连不上 | ✅ 纯诊断也发 | 大头状态页 |
| 拦截(杀软/重建 3 次失败 → 拦截页) | ✅ 要求加白 | 拦截页 |
| 程序自动修复失败 | ✅ 附重装指引 | 问题反馈 |
| 凭证库降级·备份接管成功 | ✅ | 日常页(横幅在场) |
| 未验证库验证失败(横幅①) | ✅(无 GUI 在场时横幅够不着) | 登录页 |
| 网断等网/探测循环/节流/自愈进行中/矛盾态(已自愈)/纯状态变化 | ❌ 静默 | — |

- **冷却合并(默认,数字可调)**:同类 30 分钟内合并为一条(更新原条,不重发);每类每天 ≤1 条。
- **通知点击 = 深链**:见 P1-11。
- 验收:mock 各 🔔 场景触发一次,冷却窗口内二连失败只见到一条;成功通知开关关闭后静默。

## K. 模块复用表(2026-09-10;执行硬约束——白板同标记 = 实现一份,禁止复制粘贴)

| 标记 | 模块 | 白板出现位置 | 约束 |
|---|---|---|---|
| 🅛 | 后端登录五态机 | g2.后端判断 / g3.后端返回 | 同一后端服务;首装表单/快登/检验/任务登录失败共用 |
| 🔑 | 存库凭据登录服务 | 快登 / 检验·进入检测 / 真登验证 | 同一实现·自适应:未登录→真登;已登录→chkstatus 对账;**禁注销全量**(共享会话/断网风险) |
| 📄 | 登录页 | 首装+日常(白板已合并单节点) | 一套 DOM;返回键=来路栈(从状态页来回状态页,换账号/深链回日常页) |
| 📡 | L1/L2 探测 | 探测机制带 | 全局一份·后台常驻;巡检"网"维度读其结果 |
| 🔔 | 通知服务 | J 章场景表全部 🔔 | 一份;冷却合并逻辑在此实现 |

---

## 建议执行顺序与 commit 粒度(v2)

1. **第一批(P0,一个对话可完成)**:P0-1(A1 flash 体系)→ P0-2 → P0-5 → P0-3(waiting)→ P0-4(A2 minMs)→ P1-2/P1-3/P1-4(小文案)→ P1-1(拦截页)→ P1-5(可达性包)。每项一 commit;A8(VIEW_MS 单源)作为独立小项插在 P0 后。
   > **✅ 第一批已执行完毕(2026-09-10,提交链 57e581e→feb9fad,共 11 项 commit)**:P0-1/2/5/3/4、P1-2/3/4/1/5、A8 全部落地并经内置浏览器逐项验收(AC-1/AC-2/AC-7 数据化复测、waiting≡unreachable 逐格对照、20 场景落地回归零异常、画廊改到帧已同步)。批内新拟稿 3 条已补入 H 表。P0-6(mock 对齐)不在第一批清单,P0-7 依赖第三批(P1-8/10/12),均未动。
2. **第二批(P2,建议独立对话)**:P2-1 表单合一(最大,先截图存档)→ P2-2 路由数据化 → P2-3 store 收口;每项一 commit,画廊/矩阵同步随项走。
   > **✅ 第二批(P2)全部执行完毕(2026-09-10)**:P2-1 已执行(commit 633435f);P2-2 已执行(commit 53b230e——ROUTE 查表 + netToMain 唯一压缩 + A3 formDirty 豁免落地,F-2a 路由格 20 项走查全绿,视觉零变化 A/B 验证过,证据 dev/p2-2-*);P2-3 已执行(2026-09-10——13 散全局 + 同类后补 formCtx/flashTimer/gazeHandle 收进单一 `const S`,顶格 `let` 归零,函数全保持全局;mock.js 的 IIFE 内部 `S` 与之不同作用域零冲突;画廊白名单 16 函数在位;视觉零变化经 HEAD vs 新码同缓存 A/B 0.000% 逐像素证实,画廊 58 帧 CDP 真跑与 HEAD 基线等价,证据 dev/p2-3-*)。
3. **【v2】第三批(白板定稿落,建议独立对话)**:P1-8+P1-10+P1-12(探测+状态页+快登,一组强相关,先做)→ P1-13(两态+横幅②③+总开关)→ P1-14(检验自适应)→ P1-16/P1-17(成功页矩阵+任务链)→ P1-15(故障带,后端重活,可与后端计划并行)→ P1-11(深链,契约 bump,单独拍板)。每项一 commit;F 章第 7/8 条(白板对账+复用对账)随第三批逐项走。
   > **🚧 第三批已开工(2026-09-10)**:首组 P1-8+P1-10+P1-12+P0-7 已执行并验收(探测常驻 5s 只读只发变化、v-status 四心境、快登五态、网恢复系统调起+去重+横幅⑧;279 后端测全绿,画廊 v-status 卡 7 帧+S9 重写+S10 新增,矩阵 §0-§5 已同步,本组拟稿已入 H 表【批3新增】)。执行中顺手修真 bug 一个:angry botIn 入场规则缺失致 drop/sad 心境 bot 不可见(属 P1-10 范围)。
   > **✅ P1-13 已执行并验收(2026-09-11)**:日常页两态落地 — renderMain 删 out/down 分支链(标题恒问候语,网络事实归「网络」行=📡探测喂;#main-status/st-warn/st-down/wakeProblem 删);按钮恒「立即重新登录」(断网点重登走 NET_UNREACHABLE→v-guide 实测);总开关联动三件套(S.master 镜像/横幅② master!==false 条件/toggleAuto 即刷;关=横幅②隐退+sleep,再开按真实任务态不复活,真路径实测);横幅①②③语义定稿注记(①点击→v-form 实测);画廊 v-main 卡 13 帧全绿(两态①②帧、①+⑧并存、②联动隐退;out/down 帧删)+S5/S9 故事跟进;矩阵 §0/§1/§2/§3/§4 已同步。画廊帧序/摆拍时序三处顺手修(重建失败帧 50ms 竞态、⑧帧 busy 残影、streak 帧收尾错留 v-status——帧内修复,产品码零涉及)。无新增拟稿。**P0-6 仍未动**;状态页「进入检测」暂走 v-diag 五步,**P1-14 落地后改 🔑 检验**;巡检「网」维度分诊入口属 P1-15。
   > **✅ P1-14 已执行并验收(2026-09-11)**:🔑 检验自适应服务 — `statusQuickLogin(btn,auto)` 收为三入口同一实现(K 章硬约束:状态页快登/矛盾态「进入检测」改道·不再走 v-diag/vt2 自动验证);后端零改动(复用 api.py `_login_stored_credential` 自适应语义:未登录真登/已登录 chkstatus 对账/禁注销全量);`autoVerifyOnRecover` 挂 net:state 处理器尾(不可达→可达 ∧ configured ∧ recentResult.verified===false 现拉真源 → 自动跑 🔑;vt2Busy 防叠发;vt1 在任务侧)。auto 语境:成功不拽人(交 net:state 的 ROUTE,formDirty 豁免天然成立)/ already 静默不冒领(verified 不翻,盲区留待下回)/ 被拒不在状态页只横幅①(白板 vdo→vb1)。实测 9 组全绿:对账仅 probe 无真登、未登录真登、vt2 正路径(断→恢复自动登)、已验证库不自动(streak)、vt2 失败两分支(状态页转 sad/填表静默留页)、already 静默(**实测抓到 250ms 转场竞态致冒领 flash,已修**)、主页重登+P1-12 手动 sad 零回归、down 存配置→网恢复全链。画廊 contra 帧注记+S9 补已验证库不自动+S10 第 4 步改 vt2;矩阵 §0 🔑 补注/§1 contra/§2 vt2 注/§3 #12/§4 已同步;白板 vt2 节点改「未验证库自动验证(P1-14已落)」口径(板推送待手动配方);拟稿 1 条入 H 表。**P0-6 仍未动**。下一项:P1-16/P1-17(成功页矩阵+任务链)。
   > **✅ P1-16/P1-17 已执行并验收(2026-09-11)**:成功页矩阵 + 任务①②生命周期(一组强相关,一 commit)— **P1-16**:okpg2 定稿 = 不可达语境(配置模式)标题「保存配置成功,今天/明早 HH:MM 见。」(时间动态)+ 唯一出口「回排查页」(白板 okpg2→vguide「返回(当前不可达)」,openDetail 记来路;网已恢复兜底回主页)+ 高级设置出口收掉(白板 okpg2 无此出边;锚前/降级变体原样不动);彩带口径收口 = 用户在场的验证通过(submitLadder 唯一入口;明早无人值守成功不弹彩带 — 实测 GUI 在场任务成功停 v-main 只刷状态)。**P1-17 任务①**:submitLadder 不可达分支不再硬编码 taskOk — NET_UNREACHABLE 信封不带 task_ok(契约 1.5.0 本轮不动),补一次只读 taskStatus 秒回复询分流:建成→okpg2 / 没建成→拦截页(查询失败按建成放行,横幅②兜底);拦截→重建成功→okpg2+「定时任务已修好 ✓」(白板 intercept→任务链→okpg2 链)。**P1-17 任务②**:零结构改动 — 明早任务跑(GUI 在场)走 FileWatcher 翻译的 net:state/log:appended/schedule:changed 既有管线(ROUTE 调起状态页/横幅/最近结果),登录失败不进拦截页、横幅②不亮;🔔(成功默认开/拦截要求加白/失败带原因)全在后端 ensure/notify 侧已存在,前端零改动。mock 加 `_setTaskOk(ok)`/`_runMorningTask('ok'|'fail')` 驱动钩子(头注同步)。实测 8 组全绿:okpg2 新落点+回排查页往返、任务①拦截分流+重建放行、任务②失败(状态页蓝变红+横幅⑧在场+②①不在场+日志如实)/成功(停 v-main 无彩带)、彩带回归(在场)、before_open/blocked 回归、okpg2→排查页→网恢复 vt2 全链。画廊 v-success 卡重排 8 帧(新增拦截变体+重建放行)+S5/S10 文案跟进+S11 任务链故事新增(四步浏览器过);矩阵 §0 彩带口径+NET_UNREACHABLE 行/§1 v-success 行/§3 #13/§4/§5 已同步;白板 v1/vok/vfail 三节点加「已落」注记(板推送待手动配方);拟稿 1 条入 H 表(回排查页)。**P0-6 仍未动**。下一项:P1-15(故障带,后端重活,可与后端计划并行)或 P1-11(深链,契约 bump 单独拍板);随后 P0-6 + F 章总门收尾。
   > **✅ P1-15 已执行并验收(2026-09-11)**:故障带自愈四链按契约纪律分层落地(验收条款「巡检信封字段若不足,列契约缺口单独拍板」)— **日志链(已落)**:启动巡检日志维度分诊(firstRun configured∧probe=logged_in 时拉 recentResult,今早 outcome=fail → enterStatus('contra'),白板 logskip→diag 三路之①;silent/throttled 不分诊 — 假期静默有意不打扰、节流中性留任务重试;recentResult 失败宁可漏报;②没登录→drop/③网不通→wait 两路已由 P0-7/P1-8 网态调起接管)。P1-10 的 contra 心境与 P1-14 的「进入检测」自此有了真实入口(此前只能画廊 pose)。**任务链(已落)**:后端 rebuildTask 加重试环(用户点击这一轮内未达成补试,最多 3 轮 reconcile,轮间不另睡;信封 ok=false 即一轮 3 败,pytest +2 全绿 281)— 白板 intercept2「重建直到正常」;前端拦截页+横幅② 共用计数 `S.rebuildFails`(renderBanners 正式渲染反馈版横幅,重渲稳定),三连败换反馈出口(附诊断包,白板 re3),**点击反馈出口即复位计数并复原重建入口**(横幅点击即刷回「点此重建」/拦截页先重渲再跳,返回时「重建定时任务」已恢复 — 加白杀软后的再试通路不断,再 3 败反馈入口再现);重建成功信封 ok = reconcile 内 is_task_current 判定,即「回巡检复查」结论,不加冗余复询。**库链/程序链(契约 1.6.0 缺口,本轮零动契约,清单已列 G 章)**:巡检程序/库两维无只读信封方法、自愈进度/降级状态字段缺 — 库链(备份库 JSON/自动重建×3/降级+🔔)与程序链「自动修复(自校验还原)」(打包形态 exe 损坏进程不起,现有 BRIDGE_MISSING 仪式页 P1-9 重试 + crashlog 入诊断包即现实解)整体挂起待拍板。**🔔 J 章表逐行对账(记入矩阵 §0)**:已兑现=凭据类失败/拦截加白/任务失联(每日≤1);缺口=库降级/程序修复失败/任务成功默认开/连不上纯诊断(后两项涉 ensure/notify 语义重写且「连不上也发」与假期静默拍板冲突,需用户裁决)。mock `_setTaskOk` 加第二参 sticky(重建修不好);实测 10 组全绿:分诊正样本(unverified 开机 contra+进入检测对账收场横幅①留)/负样本×2(daily 不分诊、streak 走 drop 不被劫持)/拦截页 3 败全链/横幅② 3 败全链/反馈出口复原×2(点击后计数 0、返回后重建入口恢复)/成功清零(blocked 一次重建放行回归)。画廊:contra 帧改 unverified 真开机入口、v-main unverified 两帧适配分诊、横幅② 3 败帧(删旧「重建失败」手写帧 — 单败后横幅保持原样可再点,旧行为已不存在)、S8 补第 4 步、S12 分诊故事新增(十七→十八);画廊改到 4 帧经陈列卡真跑走查全绿。矩阵 §0 巡检五维消费注+🔔 对账/§1 v-boot·v-status·v-main·v-success 行/§3 #14/§4/§5 已同步;白板 logskip/diag/intercept2/re3 加已落注记、vault/rebuild2/degrade/progfail/proggrep 加「待 1.6.0 拍板」注记(板推送待手动配方);拟稿 2 条入 H 表。**P0-6 仍未动**。下一项:P1-11(深链,契约 bump 单独拍板);随后 P0-6 + F 章总门收尾。
4. **收尾**:F 章验收总门 + H 章文案过目。
