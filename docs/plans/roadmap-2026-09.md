# Roadmap 2026-09(下一阶段规划)

> 状态:生效(常青,完成即收口)。前一阶段:ui-interaction-rework v2 已闭环(2026-09-11,F-② 21/21 + F-②a 18/18 + F-④ AC 2/2);杀软整改批 ADR-0001~0004 已落地(322 测)。本表只管**接下来**。
>
> **执行约定**(零上下文适用):测试基准 = 仓库根 `.venv-guigui` pytest,**322 只增不减**;契约变更必须 bump + 登记(docs/tech/guigui-bridge-api-v1.md);不顺手修;拍板项呈报不代拍;每单元 git commit(中文,说清做了什么为什么);subagent 接力用 `docs/gov/prompt-templates.md` T3 模板。

## P0 · 拍板解锁(**2026-09-12 六连拍已收口 5.5/6**)

| 事项 | 入口 | 拍板结果 |
|---|---|---|
| 签名路线 | docs/adr/0005 | **已拍 d**:开源 + 免费签名档(SignPath/Certum OSS)→ 转 P1 批D(第一步审计已跑) |
| 通知语义(契约 1.6.0 通知缺口) | 契约 §5 / 计划 G 章 | **已拍**:非假期模式连不上 → 自动通知(每日≤1,同类 30min 冷却);假期模式(自动判定或手动宣告,任一生效)完全静默;成功通知默认开(随 J 章拍板落 ensure 路径)→ 转 P1 批A |
| 库链(凭据备份) | docs/adr/0007(新) | **已拍「做,自动切换不弹通知」**;设计草案 ADR-0007 已落,**待用户过目** → 过目后转批C |
| 程序链(exe 自修复) | 计划 G 章 | **已拍「砍」**(exe 损坏进程不起,自修复不成立;现实解=BRIDGE_MISSING 重装指引已在线)→ 收口动作入批B |
| 深链 open_route(P1-11) | 契约 §6 既有票 | **已拍「加」** → 随批A 契约 bump 一并 |
| 停试阈值(ADR-0006) | docs/adr/0006 | **唯一待回:一个数字**(建议 3;「唤醒登录」已解释,等用户回数) |

P0 收口后:批A/B 即为纯执行,按 ADR/计划自动接力。

## P1 · 实施与验收(agent 可执行,按序)

1. **批A · 通知语义 + 契约 bump**(等 ③ 数字):ensure/notify 非假期「连不上」通知(每日≤1)+ 同类 30 分钟冷却 + 成功通知默认开;深链 open_route;契约 minor bump + 登记;H 表/矩阵联动。
2. **批B · ADR-0006 实施 + 程序链收口**(等 ③ 数字):连败停试 + 状态页降级可见 + rebuildTask 清零;白板 4 个「待 1.6.0 拍板」节点标「不做」+ 计划 G 章注记。
3. **批C · 库链实施**(等 ADR-0007 过目):按 ADR-0007 实施清单,DPAPI 备份 + 静默 failover + 状态页可见。
4. **批D · 开源 + 免费签名链**(审计已跑,见 ADR-0005 回填):前置审计清单呈报 → 用户 GitHub 建仓+push(对外动作,用户亲自确认)→ SignPath/Certum 申请(用户账号)→ build 链接 signtool → 指纹登记。
5. **安装器实机验收**(用户 5 分钟,在家可做):`guigui/Output/guigui-setup-2.0.0.exe` 装→开→卸,四任务全删;顺带收掉 package-fix-plan Task 7 旧清单。
6. **真机三件**(需在校环境):锚点窗口期实测(06:50 前后)、bind 解绑补测、返校日实跑(boot 拍豁免链)。
7. **H 表场景化呈报准备**:按「触发条件→谁看到→画面→拟稿」五段式转写 + 对照既有拍板筛冲突,并入 A6 `.sw` 范围与 P1-6 阈值两小件;呈用户过目。

## P2 · 工程化(agent 可执行)

1. **门禁自动化**(2026-09-10 已确认缺口):pre-commit 钩子跑**全量 pytest**(2026-09-12 双人独立实测 37s@326 测;早先「48 分钟」为环境干扰下的孤例,已废)+ commit 信息规范检查;方案先落 docs/plans/ 再动手。
2. **文档存量对账** ✅ 完成(2026-09-12,commit 972eed4):四份已闭环(qa-fix-plan-2026-09-07 / ui-routing-rework-2026-09-08 / ui-flow-rework-2026-09-08 / ui-interaction-rework-2026-09-09);**存疑 1 份**:guigui-package-fix-plan-2026-08-31 的 Task 7 实机 6 项人工清单在审计文档中标注「待人工验证」且从未回填,无旁证,未标已闭环,待用户裁决(该清单针对 08-31 版安装器,当前安装器已重出,验收可并入 P1-1 装机验收一并做)。
3. **1.6.0 文档收口核对**:契约 §5 所列余项与 P0 拍板结果对齐后,统一收口一次(版本/变更记录/矩阵同步)。

## 明确不做(防跑偏)

- 不做托盘/常驻、不做通用化跨校、不做 AI 写作类功能(SPEC §4 决策基线);
- 不动 legacy-v1/;不在无拍板情况下实施 ADR-0005/0006;
- 调度机制不换(审计 §5 论证),新想法先过 ADR。
