# Impeccable Design Context

## Design Context

### Users
在校大学生，住校宿舍场景。需要稳定校园网（Dr.COM）自动登录，绝大多数人不擅长技术。使用场景：早上醒来网络要能用、玩游戏/看视频断网能自动恢复、配置一次后不用再管。他们的注意力不会长时间停留在这个工具上——装好就忘。

### Brand Personality
**直接、可靠、有态度**

不讨好、不装可爱、不整花活。界面像一把好用的工具——拿起来就知道怎么用，做工扎实，细节有个性。用毛玻璃的深度感表达「这东西有层次」，用新粗野主义的粗线条和硬阴影表达「这东西不装」。

### Aesthetic Direction

**风格：毛玻璃 × 新粗野主义（Glassmorphism × Neo-Brutalism）**

毛玻璃提供层次和呼吸感，新粗野主义提供力量和辨识度。两种风格互补而非矛盾。

- **主题：** 深色模式（Dark mode only）
- **色彩系统 — 黑白灰 + 紫色，仅此而已：**
  - `#0d0d0d` 背景
  - `#171717` 卡片 / 表面（毛玻璃层）
  - `#2a2a2a` 边框
  - `#000000` 阴影
  - `#ffffff` 文字
  - `#888888` 文字次
  - `#7c3aed` 主色调（所有交互、高亮、状态指示统一使用）
- **状态系统：** 不用多色区分，靠形式——主色调圆点 `● 已连接`，灰色圆点 `● 断网`
- **阴影：** 硬偏移 4px 0 `#000`，无模糊——新粗野主义的灵魂
- **圆角：** 卡片 12px，按钮 8px，输入框 6px（不超过 16px）
- **字体：** Microsoft YaHei，标题 Bold 28px，标签 Semi-bold 13px，正文 14px
- **按钮：** 紫色填充 + 3px 深色描边 + 4px 硬偏移阴影，hover 阴影拉长弹起，active 阴影缩短按下

**参考方向（情绪板）：**
- 新粗野主义的粗线和硬阴影
- 暗色调工具 app 的克制感
- 不对称但不混乱的布局节奏

**反参考（绝对不做）：**
- 多色相配色（绿/红/琥珀等状态色）
- Tailwind 默认蓝紫渐变
- 白底 + 浅灰边框的 SaaS 模板
- 圆角 > 16px 的「友好」造型
- 软阴影 / 发光效果 / glow
- 「欢迎来到…」「开始您的旅程」等 AI 套话文案
- 纯装饰性动画

### Design Principles

1. **颜色制造深度，不要真模糊** — customtkinter 不支持 backdrop-blur，用两级色差（背景 → 卡片）模拟毛玻璃的透光层次，别硬做透明效果。
2. **硬阴影即态度** — 按钮的 4px 无模糊偏移阴影是整个视觉的语言核心，所有可交互元素都要有这个特征。
3. **一眼能操作** — 用户是大学生不是设计师，表单标签永远在输入框上方，按钮永远在底部，状态永远在最后。不需要用户思考布局。
4. **动态只服务于反馈** — 按钮 hover/active 的阴影变化是触觉反馈，不是装饰。状态切换即时无动画。不加无功能目的的动效。
5. **文案说人话** — 「立即登录」不是「开始认证流程」，「已连接」不是「网络状态：在线」。短、准、没废话。

### Component Specs

#### 卡片
- 背景: `#171717`，圆角 12px，边框 2px `#2a2a2a`，内边距 24px

#### 主按钮
- 填充 `#7c3aed`，边框 3px `#5b21b6`，圆角 8px
- 阴影: 4px 4px 0 `#000`（硬偏移）
- Hover: 阴影 6px，整体上移 2px
- Active: 阴影 2px，整体下移 2px

#### 次按钮
- 透明底，边框 2px `#7c3aed`，其余同主按钮

#### 输入框
- 背景 `#0d0d0d`（凹陷感），边框 2px `#2a2a2a`，圆角 6px
- Focus 边框 `#7c3aed`，高度 40px

#### 状态指示
- 圆点 8px + 文字 12px
- 已连接 `#7c3aed`（主色调）/ 断网/未配置 `#888888`（灰色）

#### 字号层级
- 窗口标题: Bold 28px
- Tab 名称: Bold 18px
- 字段标签: Semi-bold 13px
- 正文: Regular 14px
- 辅助/状态: Regular 12px
- 按钮: Bold 14px
